wegame:
  # WeGame 后端地址，结尾不要带 /
  # 示例：
  base_url: 

  # WeGame 核心层 API Key，scope 必须是 wegame
  # 示例：xx_xxxx_xxxxxxxxxxxxxxxx
  # 用于登录、账号绑定、账号管理等 WeGame 平台接口
  # 各游戏模块自己的 API Key 请填写到 config/config/games/<module>.yaml
  # 若留空，会自动尝试匿名令牌，仅可用于无需 API Key 的接口
  api_key: ''

  # 第三方客户端类型，只能是 bot / app / web
  # Yunzai 机器人请填写 bot
  # 示例：bot
  client_type: bot

  # 当前客户端实例 ID，可用于区分多机器人实例
  # 示例：yunzai-bot-01
  client_id: ''

  # 设备指纹，留空时会按机器信息自动生成
  # 示例：yunzai_rocom_xxxxxxxxx_abcdef1234567890
  device_fingerprint: ''

  # 请求超时时间，单位毫秒
  # 示例：15000
  request_timeout_ms: 15000

  # 登录状态轮询间隔，单位毫秒
  # 示例：2000
  login_poll_interval_ms: 2000

  # 等待扫码登录完成的超时时间，单位毫秒
  # 示例：180000
  login_timeout_ms: 180000
